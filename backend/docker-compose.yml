services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: polytrade
      POSTGRES_PASSWORD: polytrade_dev
      POSTGRES_DB: polytrade
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
